# Claude Code — secrets et overrides locaux (hors git)
.claude/settings.local.json

# Rust / Cargo
/target
**/target
Cargo.lock.bak

# Frontend React (Vite) — artefacts de build et dépendances
/frontend/node_modules
/frontend/dist
frontend/*.tsbuildinfo
**/dist

# Données runtime (volume : .sqlite + HTML des versions) — jamais commitées
/data
backend/data/
*.sqlite
*.sqlite-shm
*.sqlite-wal

# Secrets d'exécution
.env
.env.local
# Copies locales du .env de la box (secrets prod) + vhost de la box — jamais commitées.
.env.server
nginx.conf

# Couverture Rust (cargo-llvm-cov, produit localement et en CI — ne pas committer)
backend-lcov.info

# Divers
.DS_Store

# Visual brainstorming companion (session-local)
.superpowers/

# Éditeur
.vscode/

# Artefacts Playwright MCP (session-local)
.playwright-mcp/

# Prototypes HTML de test locaux — ne jamais committer (placeholder générique)
/*.local.html
