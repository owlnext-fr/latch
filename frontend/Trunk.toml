# Build SPA Yew. La sortie `dist/` sert d'input à l'étape Docker (servie en
# statique par Loco). Cf. docs/BOOTSTRAP.md §3 et §7.
[build]
target = "index.html"
dist = "dist"

[serve]
# Dev server : proxy de l'API backend pendant `trunk serve` (Phase 3).
# address = "127.0.0.1"
# port = 8080
