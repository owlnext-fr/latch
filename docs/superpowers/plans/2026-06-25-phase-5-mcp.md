# Phase 5 — Endpoint MCP + panneau Settings — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Livrer la surface MCP (`/mcp`) avec `deploy_prototype` + `list_projects` (token sur tous les tools), plus un panneau Settings admin minimal exposant les infos de branchement MCP, pour un usage de bout en bout sans shell.

**Architecture:** Adaptateur entrant fin `backend/src/mcp/` (serveur `rmcp`, gate `deploy_token` → appel des services du cœur inchangés). Hôte public via env `LATCH_PUBLIC_BASE_URL` (runtime, fail-secure), `allowed_hosts` dérivé. Endpoint admin `GET /api/settings` (sous `AdminAuth`) + page React `/admin/settings`.

**Tech Stack:** Rust / Loco (axum 0.8) / SeaORM / `rmcp ≥ 1.4` (transport Streamable HTTP) ; React / Vite / TanStack Router+Query / shadcn-ui / Vitest+MSW.

## Global Constraints

- **Le contrat fait loi** (`docs/contrat-deploy.md`). Invariants §9 non négociables et testés.
- **`rmcp ≥ 1.4.0`** impératif (CVE-2026-42559 Host-header < 1.4.0). Épinglé dans `Cargo.toml`.
- **`deploy_token` validé sur TOUS les tools** (lecture comprise, §9.3), avant tout appel au cœur.
- **Aucune réponse ne renvoie de hash** (§9.1) ; **le PIN n'apparaît jamais via MCP** ni en liste (§9.2).
- **L'auth vit dans l'adaptateur, jamais dans le cœur** (§9.4). `src/services/` ne contient ni `use axum::` ni `use loco_rs::` (garde `tests/architecture.rs`).
- **Confidentialité** : aucun nom de client réel nulle part — placeholders fictifs (`Mon Projet`, `mon-projet`, `ACME`, `demo`).
- **Fail-secure boot** : hors `Development`/`Test`, refus de démarrer si un secret/config requis est absent (jamais `matches!(…, Production)` — fail-open).
- **« Terminé »** : `cargo fmt` + `cargo clippy --all-targets -- -D warnings` verts ; `cargo nextest run` vert ; `pnpm lint`/`typecheck`/`test`/`build` verts ; `cargo deny check` vert ; mémoire (`docs/`) mise à jour.
- **Commits** : conventionnels + gitmoji (`<gitmoji> <type>: <desc>`), via `rtk git`.

---

## File Structure

**Backend**
- Create `backend/src/mcp/mod.rs` — serveur rmcp `LatchMcp`, 2 tools, `service(ctx)` builder, tests inline.
- Modify `backend/src/lib.rs` — `pub mod mcp;`.
- Modify `backend/src/web/mod.rs` — `resolve_required`, `deploy_token(ctx)`, `public_base_url(ctx)`, `host_authority(base)` + tests.
- Create `backend/src/controllers/settings.rs` — `GET /api/settings` (AdminAuth).
- Modify `backend/src/controllers/mod.rs` — `pub mod settings;`.
- Modify `backend/src/dto/mod.rs` — `SettingsResponse`.
- Modify `backend/src/openapi.rs` — enregistre `settings::get_settings` + `SettingsResponse` + tag `settings`.
- Modify `backend/src/app.rs` — `routes()` ajoute settings ; `after_routes` fail-fast + mount `/mcp`.
- Modify `backend/Cargo.toml` — dépendance `rmcp`.
- Create `backend/tests/settings_api.rs` — intégration (`401` sans session, contenu avec session).
- Modify `openapi.json` (racine, régénéré) ; `.env.example` (+ `LATCH_PUBLIC_BASE_URL`).

**Frontend**
- Create `frontend/src/hooks/use-settings.ts` — `useSettings()`.
- Create `frontend/src/routes/settings.tsx` — page Settings (Topbar + infos MCP).
- Create `frontend/src/routes/settings.test.tsx` — Vitest + MSW.
- Modify `frontend/src/router.tsx` — route `/settings`.
- Modify `frontend/src/test/utils.tsx` — `/settings` dans le harness.
- Modify `frontend/src/components/topbar.tsx` — lien Settings.
- Modify `frontend/src/i18n/locales/en.json` + `fr.json` — clés `settings.*`.
- Modify `frontend/src/api/schema.d.ts` (régénéré via `pnpm gen:api`).

**Docs (Task 8)** : `contrat-deploy.md`, `ROADMAP.md`, `INDEX.md`, `HANDOFF.md`, `ENVIRONMENT.md`, `QUIRKS.md`, `CONVENTIONS.md`.

---

## Task 1: Config helpers backend (`deploy_token`, `public_base_url`, `host_authority`)

Helpers purs, fail-secure, testés unitairement. Aucune dépendance rmcp — fondation à faible risque.

**Files:**
- Modify: `backend/src/web/mod.rs`

**Interfaces:**
- Produces:
  - `pub fn deploy_token(ctx: &AppContext) -> loco_rs::Result<String>`
  - `pub fn public_base_url(ctx: &AppContext) -> loco_rs::Result<String>` (sans `/` final)
  - `pub fn host_authority(base_url: &str) -> String` (ex. `https://latch.owlnext.fr` → `latch.owlnext.fr` ; `http://localhost:5150` → `localhost:5150`)
- Consumes : `cookie_secure(ctx)` (déjà présent, = `is_prod`).

- [ ] **Step 1: Écrire les tests** dans le module `#[cfg(test)]` existant de `backend/src/web/mod.rs` (après les tests `resolve_cookie_secret`).

```rust
    // --- resolve_required (sans plancher de longueur) ---
    use super::{host_authority, resolve_required};

    #[test]
    fn required_prod_no_env_is_err() {
        assert!(resolve_required(None, true, "dev-fallback", "DEPLOY_TOKEN").is_err());
    }

    #[test]
    fn required_prod_with_value_ok() {
        let r = resolve_required(Some("abc".to_string()), true, "dev-fallback", "X");
        assert_eq!(r.unwrap(), "abc");
    }

    #[test]
    fn required_dev_no_env_uses_fallback() {
        let r = resolve_required(None, false, "dev-fallback", "X");
        assert_eq!(r.unwrap(), "dev-fallback");
    }

    // --- host_authority ---
    #[test]
    fn host_authority_strips_scheme_and_path() {
        assert_eq!(host_authority("https://latch.owlnext.fr"), "latch.owlnext.fr");
        assert_eq!(host_authority("https://latch.owlnext.fr/"), "latch.owlnext.fr");
        assert_eq!(host_authority("http://localhost:5150/mcp"), "localhost:5150");
        assert_eq!(host_authority("latch.owlnext.fr"), "latch.owlnext.fr");
    }
```

- [ ] **Step 2: Lancer les tests, vérifier l'échec de compilation**

Run: `cargo test -p latch --lib web:: 2>&1 | head -30`
Expected: erreur « cannot find function `resolve_required` / `host_authority` ».

- [ ] **Step 3: Implémenter** dans `backend/src/web/mod.rs` (après `resolve_cookie_secret`).

```rust
/// Résout un secret/config requis SANS plancher de longueur (contrairement à
/// `resolve_cookie_secret` qui exige 64 octets pour `cookie::Key`). Fail-secure :
/// hors Development/Test, l'env var est obligatoire (pas de fallback).
fn resolve_required(
    env_value: Option<String>,
    is_prod: bool,
    dev_fallback: &str,
    label: &str,
) -> Result<String> {
    match env_value {
        Some(s) if !s.is_empty() => Ok(s),
        _ if is_prod => Err(loco_rs::Error::Message(format!(
            "{label} doit être défini en production"
        ))),
        _ => Ok(dev_fallback.to_string()),
    }
}

/// Secret partagé validé par TOUS les tools MCP (contrat §5, §9.3). Fail-secure :
/// refuse de démarrer en prod sans `DEPLOY_TOKEN`. En dev, fallback déterministe.
pub fn deploy_token(ctx: &AppContext) -> Result<String> {
    resolve_required(
        std::env::var("DEPLOY_TOKEN").ok(),
        cookie_secure(ctx),
        "dev-only-insecure-deploy-token-please-override-in-production",
        "DEPLOY_TOKEN",
    )
}

/// URL publique de base (source de vérité de l'hôte public, contrat §5/§7).
/// Normalisée sans `/` final. Fail-secure en prod ; dev → `http://localhost:<PORT>`.
pub fn public_base_url(ctx: &AppContext) -> Result<String> {
    let port = std::env::var("PORT").unwrap_or_else(|_| "5150".to_string());
    let dev_fallback = format!("http://localhost:{port}");
    let base = resolve_required(
        std::env::var("LATCH_PUBLIC_BASE_URL").ok(),
        cookie_secure(ctx),
        &dev_fallback,
        "LATCH_PUBLIC_BASE_URL",
    )?;
    Ok(base.trim_end_matches('/').to_string())
}

/// Composant hôte (`host` ou `host:port`) d'une URL de base, pour `allowed_hosts`
/// (rmcp ≥ 1.4, validation du `Host` header). Parsing minimal (pas de crate `url`).
pub fn host_authority(base_url: &str) -> String {
    let without_scheme = base_url
        .split_once("://")
        .map_or(base_url, |(_, rest)| rest);
    without_scheme
        .split(['/', '?', '#'])
        .next()
        .unwrap_or(without_scheme)
        .to_string()
}
```

- [ ] **Step 4: Lancer les tests, vérifier le succès**

Run: `cargo test -p latch --lib web::`
Expected: PASS (tous les tests `web::`).

- [ ] **Step 5: fmt + clippy + commit**

```bash
cargo fmt --all
cargo clippy -p latch --all-targets -- -D warnings
rtk git add backend/src/web/mod.rs
rtk git commit -m "✨ feat(mcp): helpers deploy_token / public_base_url / host_authority (fail-secure)"
```

---

## Task 2: Dépendance rmcp + squelette `mcp/` qui compile et se monte

Ajoute `rmcp`, crée `LatchMcp` avec un `ServerHandler` minimal (tools vides) et `service(ctx)`, monte sous `/mcp`. Objectif : faire **compiler et booter** avant d'ajouter la logique. C'est ici qu'on absorbe la friction d'API rmcp (vérifier chaque symbole via `cargo build`).

**Files:**
- Modify: `backend/Cargo.toml`
- Create: `backend/src/mcp/mod.rs`
- Modify: `backend/src/lib.rs`
- Modify: `backend/src/app.rs`

**Interfaces:**
- Produces:
  - `pub struct LatchMcp { db, storage, deploy_token, public_base_url, tool_router }`
  - `pub fn LatchMcp::new(db: DatabaseConnection, storage: Arc<dyn Storage>, deploy_token: String, public_base_url: String) -> Self`
  - `pub fn service(ctx: &AppContext) -> loco_rs::Result<StreamableHttpService<LatchMcp, LocalSessionManager>>`
- Consumes : `web::{deploy_token, public_base_url, host_authority, storage_from_ctx}` (Task 1).

- [ ] **Step 1: Ajouter la dépendance rmcp**

Dans `backend/Cargo.toml`, sous `[dependencies]` (après `time = …`) :

```toml
# MCP (contrat §5). >= 1.4.0 impératif : CVE-2026-42559 (Host-header < 1.4.0).
rmcp = { version = "1.4", features = ["server", "macros", "transport-streamable-http-server"] }
```

Puis épingler la version exacte résolue :

```bash
cargo add rmcp --features server,macros,transport-streamable-http-server -p latch
# Vérifier que la version retenue est >= 1.4.0 ; sinon forcer: cargo update -p rmcp
cargo tree -p latch -i rmcp | head -3
```

Note de vérification : si un nom de feature est rejeté, lister les features réelles avec
`cargo info rmcp` (ou la page docs.rs) — les noms probables sont `server`,
`macros`, `transport-streamable-http-server`. La macro `#[tool]` tire `schemars` transitivement.

- [ ] **Step 2: Déclarer le module**

`backend/src/lib.rs` — ajouter après `pub mod initializers;` (ordre alpha) :

```rust
pub mod mcp;
```

- [ ] **Step 3: Écrire le squelette** `backend/src/mcp/mod.rs`

```rust
//! Adaptateur entrant "MCP" — fin (contrat §1, §5). Chaque tool valide le
//! `deploy_token` (services::security::secure_compare) AVANT d'appeler un service
//! du cœur. Aucune logique métier ici ; le cœur reste agnostique HTTP/MCP.

use std::sync::Arc;

use loco_rs::app::AppContext;
use rmcp::handler::server::tool::ToolRouter;
use rmcp::handler::server::wrapper::{Json, Parameters};
use rmcp::model::{ErrorData, ServerCapabilities, ServerInfo};
use rmcp::transport::streamable_http_server::session::local::LocalSessionManager;
use rmcp::transport::streamable_http_server::StreamableHttpService;
use rmcp::transport::streamable_http_server::tower::StreamableHttpServerConfig;
use rmcp::{schemars, tool, tool_handler, tool_router, ServerHandler};
use sea_orm::DatabaseConnection;
use serde::{Deserialize, Serialize};

use crate::services::deploy::DeployService;
use crate::services::errors::CoreError;
use crate::services::projects::ProjectsService;
use crate::services::security::secure_compare;
use crate::services::storage::Storage;

/// Serveur d'outils MCP. Porte des dépendances concrètes (pas l'`AppContext`) :
/// l'auth/config est résolue au montage (`service`), le serveur reste testable
/// sans booter Loco.
#[derive(Clone)]
pub struct LatchMcp {
    db: DatabaseConnection,
    storage: Arc<dyn Storage>,
    deploy_token: String,
    public_base_url: String,
    tool_router: ToolRouter<LatchMcp>,
}

#[tool_router]
impl LatchMcp {
    pub fn new(
        db: DatabaseConnection,
        storage: Arc<dyn Storage>,
        deploy_token: String,
        public_base_url: String,
    ) -> Self {
        Self {
            db,
            storage,
            deploy_token,
            public_base_url,
            tool_router: Self::tool_router(),
        }
    }
}

#[tool_handler]
impl ServerHandler for LatchMcp {
    fn get_info(&self) -> ServerInfo {
        ServerInfo {
            instructions: Some(
                "latch — déploiement de prototypes HTML. Outils : deploy_prototype, \
                 list_projects. Chaque appel exige le deploy_token."
                    .to_string(),
            ),
            capabilities: ServerCapabilities::builder().enable_tools().build(),
            ..Default::default()
        }
    }
}

/// Mappe une erreur du cœur vers une erreur de tool MCP (jamais de fuite de détail DB/IO).
fn map_core_err(e: CoreError) -> ErrorData {
    match e {
        CoreError::NotFound => ErrorData::invalid_params("projet inconnu", None),
        CoreError::Validation(msg) => ErrorData::invalid_params(msg, None),
        CoreError::Db(_) | CoreError::Io(_) => ErrorData::internal_error("erreur interne", None),
    }
}

/// Construit le service HTTP MCP montable dans axum (`nest_service("/mcp", …)`).
/// `allowed_hosts` dérivé de `LATCH_PUBLIC_BASE_URL` (source unique, défense Host-header).
pub fn service(
    ctx: &AppContext,
) -> loco_rs::Result<StreamableHttpService<LatchMcp, LocalSessionManager>> {
    let db = ctx.db.clone();
    let storage = crate::web::storage_from_ctx(ctx);
    let token = crate::web::deploy_token(ctx)?;
    let base = crate::web::public_base_url(ctx)?;
    let host = crate::web::host_authority(&base);

    let config = StreamableHttpServerConfig::default().with_allowed_hosts([host]);

    let factory = move || {
        Ok(LatchMcp::new(
            db.clone(),
            storage.clone(),
            token.clone(),
            base.clone(),
        ))
    };

    Ok(StreamableHttpService::new(
        factory,
        Arc::new(LocalSessionManager::default()),
        config,
    ))
}
```

> Notes de vérification rmcp (résoudre au `cargo build`, l'API a bougé en 1.x ; cf. Context7) :
> - **Chemins d'import** : si `…::tower::StreamableHttpServerConfig` ne résout pas, le config
>   est ré-exporté à `rmcp::transport::streamable_http_server::StreamableHttpServerConfig`.
>   `LocalSessionManager` : `…::session::local::LocalSessionManager`.
> - **`StreamableHttpServerConfig::default()`** : si `Default` n'est pas dérivé, utiliser le
>   chemin builder documenté ou remplir les champs publics ; `with_allowed_hosts(impl IntoIterator<Item = impl Into<String>>)` est confirmé par la doc.
> - **`ErrorData::invalid_params` / `internal_error`** prennent `(message: impl Into<String/Cow>, data: Option<serde_json::Value>)` — confirmé par les impls par défaut du handler.
> - **`#[tool_router]` + `#[tool_handler]`** : le 1er génère l'assoc-fn `Self::tool_router()` et requiert le champ `tool_router: ToolRouter<Self>` ; le 2nd câble `call_tool`/`list_tools` dessus.

- [ ] **Step 4: Monter dans `app.rs`**

`backend/src/app.rs`, dans `after_routes`, **avant** `Ok(router)` (après le bloc Swagger) :

```rust
        // MCP (contrat §5) : fail-fast des secrets/config au boot (comme unlock_secret),
        // puis montage du service Streamable HTTP sous /mcp. allowed_hosts dérivé de
        // LATCH_PUBLIC_BASE_URL (défense Host-header, CVE rmcp < 1.4.0).
        crate::web::deploy_token(ctx)?;
        let mcp = crate::mcp::service(ctx)?;
        let router = router.nest_service("/mcp", mcp);
```

(`public_base_url(ctx)?` est déjà appelé dans `mcp::service` → fail-fast couvert.)

- [ ] **Step 5: Build + boot test**

Run: `cargo build -p latch`
Expected: compile (corriger les chemins rmcp si besoin selon les notes du Step 3).

Run: `cargo nextest run -p latch --test admin_api boots_with_session_layer_and_serves_health`
Expected: PASS (l'app boote avec `/mcp` monté ; en Test, fail-secure inactif → fallbacks dev).

- [ ] **Step 6: fmt + clippy + commit**

```bash
cargo fmt --all
cargo clippy -p latch --all-targets -- -D warnings
rtk git add backend/Cargo.toml backend/Cargo.lock backend/src/lib.rs backend/src/mcp/mod.rs backend/src/app.rs
rtk git commit -m "✨ feat(mcp): squelette serveur rmcp Streamable HTTP monté sous /mcp (allowed_hosts dérivé)"
```

---

## Task 3: Tool `deploy_prototype`

Gate token → `get_by_slug` (erreur si inconnu) → `deploy()` (activate défaut `true`) → `{url, version, code_protected}`. Tests inline (réutilisent `test_support::test_db` + `FsStorage` tempdir, comme `deploy.rs`).

**Files:**
- Modify: `backend/src/mcp/mod.rs`

**Interfaces:**
- Produces (dans `impl LatchMcp`, bloc `#[tool_router]`) :
  - `async fn deploy_prototype(&self, Parameters<DeployArgs>) -> Result<Json<DeployResult>, ErrorData>`
  - `struct DeployArgs { slug, html, deploy_token, activate: Option<bool> }`
  - `pub struct DeployResult { url: String, version: i32, code_protected: bool }`
  - `fn check_token(&self, &str) -> Result<(), ErrorData>`

- [ ] **Step 1: Écrire les tests** — ajouter en bas de `backend/src/mcp/mod.rs` :

```rust
#[cfg(test)]
#[allow(clippy::unwrap_used, clippy::expect_used)]
mod tests {
    use std::sync::Arc;

    use tempfile::TempDir;

    use super::*;
    use crate::services::projects::{CreateProject, ProjectsService};
    use crate::services::storage::FsStorage;
    use crate::services::test_support::test_db;

    const TOKEN: &str = "test-deploy-token";

    fn mcp(db: DatabaseConnection, dir: &TempDir) -> LatchMcp {
        let storage: Arc<dyn Storage> = Arc::new(FsStorage::new(dir.path().to_path_buf()));
        LatchMcp::new(db, storage, TOKEN.to_string(), "https://demo.test".to_string())
    }

    async fn make_project(db: &DatabaseConnection, code: bool) -> crate::models::_entities::projects::Model {
        ProjectsService::new(db.clone())
            .create(CreateProject {
                name: "Mon Projet".to_string(),
                brand_name: None,
                code_enabled: code,
                pin: if code { Some("123456".to_string()) } else { None },
            })
            .await
            .unwrap()
    }

    #[tokio::test]
    async fn deploy_rejects_bad_token() {
        let db = test_db().await;
        let dir = tempfile::tempdir().unwrap();
        let p = make_project(&db, false).await;
        let m = mcp(db, &dir);
        let res = m
            .deploy_prototype(Parameters(DeployArgs {
                slug: p.slug.clone(),
                html: "<h1>x</h1>".to_string(),
                deploy_token: "WRONG".to_string(),
                activate: None,
            }))
            .await;
        assert!(res.is_err(), "token invalide doit être rejeté");
    }

    #[tokio::test]
    async fn deploy_unknown_slug_is_error() {
        let db = test_db().await;
        let dir = tempfile::tempdir().unwrap();
        let m = mcp(db, &dir);
        let res = m
            .deploy_prototype(Parameters(DeployArgs {
                slug: "nope-xxxxxxxx".to_string(),
                html: "<h1>x</h1>".to_string(),
                deploy_token: TOKEN.to_string(),
                activate: None,
            }))
            .await;
        assert!(res.is_err(), "slug inconnu doit être une erreur (pas d'auto-création)");
    }

    #[tokio::test]
    async fn deploy_creates_version_and_activates_by_default() {
        let db = test_db().await;
        let dir = tempfile::tempdir().unwrap();
        let p = make_project(&db, true).await;
        let m = mcp(db.clone(), &dir);
        let Json(out) = m
            .deploy_prototype(Parameters(DeployArgs {
                slug: p.slug.clone(),
                html: "<h1>hi</h1>".to_string(),
                deploy_token: TOKEN.to_string(),
                activate: None, // défaut = true
            }))
            .await
            .unwrap();

        assert_eq!(out.version, 1);
        assert_eq!(out.code_protected, true);
        assert_eq!(out.url, format!("https://demo.test/c/{}", p.slug));
        // pointeur flippé
        let reloaded = crate::models::_entities::projects::Entity::find_by_id(p.id)
            .one(&db)
            .await
            .unwrap()
            .unwrap();
        assert!(reloaded.active_version_id.is_some(), "activate défaut → pointeur posé");
    }

    #[tokio::test]
    async fn deploy_without_activate_leaves_pointer_null() {
        let db = test_db().await;
        let dir = tempfile::tempdir().unwrap();
        let p = make_project(&db, false).await;
        let m = mcp(db.clone(), &dir);
        let Json(out) = m
            .deploy_prototype(Parameters(DeployArgs {
                slug: p.slug.clone(),
                html: "<h1>draft</h1>".to_string(),
                deploy_token: TOKEN.to_string(),
                activate: Some(false),
            }))
            .await
            .unwrap();
        assert_eq!(out.version, 1);
        assert_eq!(out.code_protected, false);
        let reloaded = crate::models::_entities::projects::Entity::find_by_id(p.id)
            .one(&db)
            .await
            .unwrap()
            .unwrap();
        assert!(reloaded.active_version_id.is_none(), "activate=false → pas de bascule");
    }
}
```

Ajouter l'import `use sea_orm::EntityTrait;` en tête des tests si nécessaire (pour `find_by_id`).

- [ ] **Step 2: Lancer, vérifier l'échec**

Run: `cargo nextest run -p latch mcp:: 2>&1 | head -20`
Expected: échec de compilation (`deploy_prototype`, `DeployArgs`, `DeployResult` inconnus).

- [ ] **Step 3: Implémenter** — dans `backend/src/mcp/mod.rs`, ajouter les types (après les imports) et la méthode (dans le bloc `#[tool_router] impl LatchMcp`, après `new`).

Types (niveau module) :

```rust
#[derive(Debug, Deserialize, schemars::JsonSchema)]
struct DeployArgs {
    /// Slug public du projet (doit déjà exister, créé via /admin).
    slug: String,
    /// HTML mono-fichier complet du prototype.
    html: String,
    /// Secret de déploiement (validé contre DEPLOY_TOKEN).
    deploy_token: String,
    /// Activer immédiatement la version déployée (défaut : true).
    #[serde(default)]
    activate: Option<bool>,
}

#[derive(Debug, Serialize, schemars::JsonSchema)]
pub struct DeployResult {
    /// URL publique stable du prototype (sert toujours la version active).
    pub url: String,
    /// Numéro de la version créée.
    pub version: i32,
    /// `true` si le projet exige un code d'accès (un PIN sera demandé au visiteur).
    pub code_protected: bool,
}
```

Méthode + helper (dans le bloc `#[tool_router] impl LatchMcp`) :

```rust
    /// Valide le `deploy_token` à temps constant (contrat §9.3). Avant tout appel au cœur.
    fn check_token(&self, provided: &str) -> Result<(), ErrorData> {
        if secure_compare(provided, &self.deploy_token) {
            Ok(())
        } else {
            Err(ErrorData::invalid_params("deploy_token invalide", None))
        }
    }

    #[tool(
        description = "Déploie un prototype HTML mono-fichier comme nouvelle version d'un \
                       projet EXISTANT (identifié par son slug). Le projet doit avoir été créé \
                       au préalable dans l'admin. Active la version par défaut."
    )]
    async fn deploy_prototype(
        &self,
        Parameters(args): Parameters<DeployArgs>,
    ) -> Result<Json<DeployResult>, ErrorData> {
        self.check_token(&args.deploy_token)?;

        let projects = ProjectsService::new(self.db.clone());
        let project = projects.get_by_slug(&args.slug).await.map_err(map_core_err)?;

        let activate = args.activate.unwrap_or(true);
        let deploy = DeployService::new(self.db.clone(), self.storage.clone());
        let version = deploy
            .deploy(project.id, &args.html, activate)
            .await
            .map_err(map_core_err)?;

        Ok(Json(DeployResult {
            url: format!("{}/c/{}", self.public_base_url, project.slug),
            version: version.n,
            code_protected: project.code_enabled,
        }))
    }
```

- [ ] **Step 4: Lancer, vérifier le succès**

Run: `cargo nextest run -p latch mcp::`
Expected: PASS (4 tests).

- [ ] **Step 5: fmt + clippy + commit**

```bash
cargo fmt --all
cargo clippy -p latch --all-targets -- -D warnings
rtk git add backend/src/mcp/mod.rs
rtk git commit -m "✨ feat(mcp): tool deploy_prototype (gate token, slug préexistant, activate défaut true)"
```

---

## Task 4: Tool `list_projects`

Gate token → `list_with_versions` → liste `{slug, name, code_protected, active_version}`. Aucun PIN, aucun hash, aucun id interne (§9.1/§9.2).

**Files:**
- Modify: `backend/src/mcp/mod.rs`

**Interfaces:**
- Produces :
  - `async fn list_projects(&self, Parameters<ListArgs>) -> Result<Json<Vec<ProjectSummary>>, ErrorData>`
  - `struct ListArgs { deploy_token: String }`
  - `pub struct ProjectSummary { slug, name, code_protected, active_version: Option<i32> }`
- Consumes : `check_token` (Task 3), `ProjectsService::list_with_versions()` → `Vec<(projects::Model, Vec<versions::Model>)>`.

- [ ] **Step 1: Écrire les tests** — ajouter dans le `mod tests` de `mcp/mod.rs` :

```rust
    #[tokio::test]
    async fn list_rejects_bad_token() {
        let db = test_db().await;
        let dir = tempfile::tempdir().unwrap();
        let m = mcp(db, &dir);
        let res = m
            .list_projects(Parameters(ListArgs { deploy_token: "WRONG".to_string() }))
            .await;
        assert!(res.is_err(), "list_projects doit gater le token (§9.3)");
    }

    #[tokio::test]
    async fn list_returns_summaries_without_pin() {
        let db = test_db().await;
        let dir = tempfile::tempdir().unwrap();
        let p = make_project(&db, true).await; // code activé, pin 123456
        let m = mcp(db, &dir);
        let Json(out) = m
            .list_projects(Parameters(ListArgs { deploy_token: TOKEN.to_string() }))
            .await
            .unwrap();

        assert_eq!(out.len(), 1);
        assert_eq!(out[0].slug, p.slug);
        assert_eq!(out[0].code_protected, true);
        assert_eq!(out[0].active_version, None);
        // Invariant §9.2 : le PIN ne doit JAMAIS transiter par MCP.
        let json = serde_json::to_string(&out).unwrap();
        assert!(!json.contains("123456"), "le PIN ne doit pas apparaître via MCP (§9.2)");
        assert!(!json.contains("\"pin\""), "pas de champ pin dans le résumé MCP");
    }
```

- [ ] **Step 2: Lancer, vérifier l'échec**

Run: `cargo nextest run -p latch mcp::list 2>&1 | head -20`
Expected: échec de compilation (`list_projects`, `ListArgs`, `ProjectSummary` inconnus).

- [ ] **Step 3: Implémenter** — types (niveau module) + méthode (dans `#[tool_router] impl LatchMcp`).

```rust
#[derive(Debug, Deserialize, schemars::JsonSchema)]
struct ListArgs {
    /// Secret de déploiement (validé contre DEPLOY_TOKEN).
    deploy_token: String,
}

#[derive(Debug, Serialize, schemars::JsonSchema)]
pub struct ProjectSummary {
    /// Slug public — à passer à `deploy_prototype`.
    pub slug: String,
    pub name: String,
    /// `true` si le projet exige un code d'accès.
    pub code_protected: bool,
    /// Numéro de la version active, ou `null` si jamais déployé.
    pub active_version: Option<i32>,
}
```

```rust
    #[tool(
        description = "Liste les projets déployables (slug, nom, protection par code, n° de \
                       version active). Ne renvoie jamais de PIN ni de secret."
    )]
    async fn list_projects(
        &self,
        Parameters(args): Parameters<ListArgs>,
    ) -> Result<Json<Vec<ProjectSummary>>, ErrorData> {
        self.check_token(&args.deploy_token)?;

        let projects = ProjectsService::new(self.db.clone());
        let rows = projects.list_with_versions().await.map_err(map_core_err)?;

        let out = rows
            .iter()
            .map(|(p, vers)| ProjectSummary {
                slug: p.slug.clone(),
                name: p.name.clone(),
                code_protected: p.code_enabled,
                active_version: p
                    .active_version_id
                    .and_then(|aid| vers.iter().find(|v| v.id == aid).map(|v| v.n)),
            })
            .collect();

        Ok(Json(out))
    }
```

- [ ] **Step 4: Lancer, vérifier le succès**

Run: `cargo nextest run -p latch mcp::`
Expected: PASS (6 tests au total).

- [ ] **Step 5: fmt + clippy + commit**

```bash
cargo fmt --all
cargo clippy -p latch --all-targets -- -D warnings
rtk git add backend/src/mcp/mod.rs
rtk git commit -m "✨ feat(mcp): tool list_projects (gate token, résumé sans PIN ni hash §9.2)"
```

---

## Task 5: Endpoint `GET /api/settings` (backend)

DTO `SettingsResponse` + contrôleur sous `AdminAuth` + enregistrement openapi + régénération `openapi.json`/`schema.d.ts` + test d'intégration.

**Files:**
- Modify: `backend/src/dto/mod.rs`
- Create: `backend/src/controllers/settings.rs`
- Modify: `backend/src/controllers/mod.rs`
- Modify: `backend/src/openapi.rs`
- Modify: `backend/src/app.rs`
- Create: `backend/tests/settings_api.rs`
- Modify: `openapi.json`, `frontend/src/api/schema.d.ts` (régénérés)

**Interfaces:**
- Produces :
  - `pub struct SettingsResponse { deploy_token: String, mcp_url: String, public_base_url: String }`
  - `controllers::settings::routes() -> Routes` (`GET /api/settings`)
- Consumes : `AdminAuth`, `web::{deploy_token, public_base_url}`.

- [ ] **Step 1: Écrire le test d'intégration** `backend/tests/settings_api.rs`

```rust
#![allow(clippy::unwrap_used, clippy::expect_used)]

use latch::app::App;
use loco_rs::testing::prelude::*;
use serial_test::serial;

/// Sans session, `/api/settings` doit répondre 401 (AdminAuth).
#[tokio::test]
#[serial]
async fn settings_is_401_without_session() {
    request::<App, _, _>(|request, _ctx| async move {
        let res = request.get("/api/settings").await;
        assert_eq!(res.status_code(), 401);
    })
    .await;
}

/// Authentifié, `/api/settings` renvoie deploy_token + mcp_url + public_base_url.
#[tokio::test]
#[serial]
async fn settings_returns_mcp_info_when_authenticated() {
    std::env::set_var("ADMIN_USER", "admin");
    std::env::set_var("ADMIN_PASS", "s3cret");
    std::env::set_var("DEPLOY_TOKEN", "tok-abc-123");
    std::env::set_var("LATCH_PUBLIC_BASE_URL", "https://demo.test");
    let config = RequestConfigBuilder::new().save_cookies(true).build();
    request_with_config::<App, _, _>(config, |request, _ctx| async move {
        request
            .post("/api/login")
            .json(&serde_json::json!({"user": "admin", "pass": "s3cret"}))
            .await;
        let res = request.get("/api/settings").await;
        assert_eq!(res.status_code(), 200);
        let body: serde_json::Value = res.json();
        assert_eq!(body["deploy_token"], "tok-abc-123");
        assert_eq!(body["public_base_url"], "https://demo.test");
        assert_eq!(body["mcp_url"], "https://demo.test/mcp");
    })
    .await;
}
```

- [ ] **Step 2: Lancer, vérifier l'échec**

Run: `cargo nextest run -p latch --test settings_api 2>&1 | head -20`
Expected: 404 sur `/api/settings` (route inexistante) → assertions échouent.

- [ ] **Step 3: Ajouter le DTO** dans `backend/src/dto/mod.rs` (après `ActivateResponse`) :

```rust
/// Réponse de `GET /api/settings` — infos de branchement MCP pour l'admin.
/// Expose `deploy_token` (secret applicatif) à un admin AUTHENTIFIÉ uniquement
/// (même logique que le PIN au détail). Jamais via MCP, jamais en liste de projets.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, ToSchema)]
pub struct SettingsResponse {
    pub deploy_token: String,
    pub mcp_url: String,
    pub public_base_url: String,
}
```

- [ ] **Step 4: Créer le contrôleur** `backend/src/controllers/settings.rs`

```rust
//! Adaptateur entrant "settings admin". Lecture seule (GET → pas de garde Origin).
//! Sous `AdminAuth` : expose les infos de branchement MCP, dont le `deploy_token`,
//! à un admin AUTHENTIFIÉ (contrat §5/§9 : acceptable, l'admin a déjà le contrôle total).

use loco_rs::prelude::*;

use crate::controllers::auth::AdminAuth;
use crate::dto::SettingsResponse;

/// GET /api/settings — infos de branchement du connecteur MCP Claude.
#[utoipa::path(
    get, path = "/api/settings", tag = "settings",
    responses(
        (status = 200, description = "Infos MCP (deploy_token + URLs)", body = SettingsResponse),
        (status = 401, description = "Non authentifié")
    )
)]
#[debug_handler]
async fn get_settings(_auth: AdminAuth, State(ctx): State<AppContext>) -> Result<Response> {
    let public_base_url = crate::web::public_base_url(&ctx)?;
    let deploy_token = crate::web::deploy_token(&ctx)?;
    let mcp_url = format!("{public_base_url}/mcp");
    format::json(SettingsResponse {
        deploy_token,
        mcp_url,
        public_base_url,
    })
}

pub fn routes() -> Routes {
    Routes::new().prefix("/api").add("/settings", get(get_settings))
}
```

- [ ] **Step 5: Déclarer le module + monter la route**

`backend/src/controllers/mod.rs` — ajouter (ordre alpha, après `pub mod serve_ratelimit;`) :

```rust
pub mod settings;
```

`backend/src/app.rs` — dans `routes()`, après `.add_route(controllers::serve::routes())` :

```rust
            .add_route(controllers::settings::routes())
```

- [ ] **Step 6: Enregistrer dans l'OpenAPI**

`backend/src/openapi.rs` :
- `use crate::controllers::{admin, auth, serve};` → ajouter `settings` : `use crate::controllers::{admin, auth, serve, settings};`
- dans `paths(…)`, après `serve::public_meta,` : `settings::get_settings,`
- dans `components(schemas(…))`, après `dto::PublicMeta,` : `dto::SettingsResponse,`
- dans `tags(…)`, ajouter : `(name = "settings", description = "Configuration admin (infos MCP)"),`
- dans le test `document_contains_all_paths`, ajouter `"/api/settings",` à la liste.

- [ ] **Step 7: Régénérer openapi.json + schema.d.ts**

```bash
UPDATE_OPENAPI=1 cargo test -p latch --test openapi_drift
cd frontend && pnpm gen:api && cd ..
```

- [ ] **Step 8: Lancer les tests**

Run: `cargo nextest run -p latch --test settings_api --test openapi_drift`
Expected: PASS (3 tests).

- [ ] **Step 9: fmt + clippy + commit**

```bash
cargo fmt --all
cargo clippy -p latch --all-targets -- -D warnings
rtk git add backend/src/dto/mod.rs backend/src/controllers/settings.rs backend/src/controllers/mod.rs backend/src/openapi.rs backend/src/app.rs backend/tests/settings_api.rs openapi.json frontend/src/api/schema.d.ts
rtk git commit -m "✨ feat(settings): GET /api/settings (AdminAuth) expose infos MCP (deploy_token + urls)"
```

---

## Task 6: Panneau Settings React

Hook `useSettings`, page `/admin/settings` (Topbar + `deploy_token` via `PinField` + `mcp_url`/`public_base_url` copiables), lien dans la topbar, i18n FR/EN, test Vitest+MSW.

**Files:**
- Create: `frontend/src/hooks/use-settings.ts`
- Create: `frontend/src/routes/settings.tsx`
- Create: `frontend/src/routes/settings.test.tsx`
- Modify: `frontend/src/router.tsx`
- Modify: `frontend/src/test/utils.tsx`
- Modify: `frontend/src/components/topbar.tsx`
- Modify: `frontend/src/i18n/locales/en.json`, `frontend/src/i18n/locales/fr.json`

**Interfaces:**
- Produces : `useSettings()` (TanStack Query, GET `/api/settings`) ; `SettingsPage` (export nommé).
- Consumes : `api` (client typé), `PinField`, `CopyButton`, `Topbar`, types `components['schemas']['SettingsResponse']`.

- [ ] **Step 1: Écrire le test** `frontend/src/routes/settings.test.tsx`

```tsx
import { describe, it, expect, beforeEach } from 'vitest'
import { screen, waitFor } from '@testing-library/react'
import userEvent from '@testing-library/user-event'
import { http, HttpResponse } from 'msw'
import { server } from '@/test/msw'
import { renderWithRouter } from '@/test/utils'

function mockSettings() {
  server.use(
    http.get(`${window.location.origin}/api/settings`, () =>
      HttpResponse.json({
        deploy_token: 'tok-abc-123',
        mcp_url: 'https://demo.test/mcp',
        public_base_url: 'https://demo.test',
      }),
    ),
  )
}

describe('SettingsPage', () => {
  beforeEach(() => server.resetHandlers())

  it('masks the deploy token by default and reveals it on click', async () => {
    mockSettings()
    renderWithRouter('/settings')

    // mcp_url visible directement
    await waitFor(() => expect(screen.getByText('https://demo.test/mcp')).toBeInTheDocument())
    // token masqué : la valeur en clair n'est pas affichée tant qu'on ne révèle pas
    expect(screen.queryByText('tok-abc-123')).not.toBeInTheDocument()

    const reveal = screen.getByRole('button', { name: /reveal|révéler/i })
    await userEvent.click(reveal)
    expect(screen.getByText('tok-abc-123')).toBeInTheDocument()
  })
})
```

- [ ] **Step 2: Lancer, vérifier l'échec**

Run: `cd frontend && pnpm test -- settings`
Expected: FAIL (route `/settings` et page absentes).

- [ ] **Step 3: Créer le hook** `frontend/src/hooks/use-settings.ts`

```ts
import { useQuery } from '@tanstack/react-query'
import { api } from '@/api/client'

export function useSettings() {
  return useQuery({
    queryKey: ['settings'],
    queryFn: async () => {
      const { data, error } = await api.GET('/api/settings')
      if (error) throw new Error('settings')
      return data
    },
  })
}
```

- [ ] **Step 4: Créer la page** `frontend/src/routes/settings.tsx`

```tsx
import { useTranslation } from 'react-i18next'
import { Topbar } from '@/components/topbar'
import { CopyButton } from '@/components/copy-button'
import { PinField } from '@/components/pin-field'
import { useSettings } from '@/hooks/use-settings'

export function SettingsPage() {
  const { t } = useTranslation()
  const { data, isLoading } = useSettings()

  return (
    <div className="min-h-screen">
      <Topbar />
      <main className="mx-auto max-w-2xl px-4 py-8">
        <h1 className="text-xl font-bold">{t('settings.title')}</h1>
        <p className="text-muted-foreground mt-1 text-sm">{t('settings.mcp_intro')}</p>

        {isLoading || !data ? (
          <p className="text-muted-foreground mt-6 text-sm">{t('common.loading')}</p>
        ) : (
          <dl className="mt-6 space-y-6">
            <div>
              <dt className="text-sm font-medium">{t('settings.mcp_url')}</dt>
              <dd className="mt-1 flex items-center gap-2">
                <span className="font-mono text-sm">{data.mcp_url}</span>
                <CopyButton text={data.mcp_url} ariaLabel={t('settings.copy_mcp_url')} />
              </dd>
            </div>
            <div>
              <dt className="text-sm font-medium">{t('settings.deploy_token')}</dt>
              <dd className="mt-1">
                <PinField pin={data.deploy_token} />
              </dd>
            </div>
            <div>
              <dt className="text-sm font-medium">{t('settings.public_base_url')}</dt>
              <dd className="mt-1 font-mono text-sm">{data.public_base_url}</dd>
            </div>
          </dl>
        )}
      </main>
    </div>
  )
}
```

> `PinField` masque n'importe quelle chaîne (`••••••`), expose œil de révélation + CopyButton —
> réutilisable pour le token (pas seulement un PIN à 6 chiffres). Les aria-labels viennent des
> clés `detail.reveal_pin`/`detail.hide_pin`/`detail.copy_pin_aria` (déjà présentes).

- [ ] **Step 5: Enregistrer la route** dans `frontend/src/router.tsx`

```tsx
import { SettingsPage } from './routes/settings'
// …
const settingsRoute = createRoute({
  getParentRoute: () => rootRoute,
  path: '/settings',
  component: SettingsPage,
})

const routeTree = rootRoute.addChildren([loginRoute, listRoute, detailRoute, settingsRoute])
```

- [ ] **Step 6: Étendre le harness de test** `frontend/src/test/utils.tsx`

- Ajouter l'import : `import { SettingsPage } from '@/routes/settings'`
- Élargir le type : `type TestPath = '/login' | '/' | '/settings'`
- Dans `buildTestRouter`, enregistrer la route et l'ajouter aux enfants :

```tsx
  const settingsRoute = createRoute({
    getParentRoute: () => rootRoute,
    path: '/settings',
    component: SettingsPage,
  })
  const routeTree = rootRoute.addChildren([loginRoute, listRoute, settingsRoute])
```

- [ ] **Step 7: Lien dans la topbar** `frontend/src/components/topbar.tsx`

- Ajouter l'import icône : `import { Settings } from 'lucide-react'`
- Dans le `<div className="flex items-center gap-2">`, avant `<LocaleSwitcher />` :

```tsx
        <Button
          type="button"
          variant="ghost"
          size="icon-sm"
          aria-label={t('settings.title')}
          onClick={() => {
            router.navigate({ to: '/settings' })
          }}
        >
          <Settings />
        </Button>
```

- [ ] **Step 8: Clés i18n** — ajouter un bloc `"settings"` dans `frontend/src/i18n/locales/en.json` :

```json
  "settings": {
    "title": "Settings",
    "mcp_intro": "Connect Claude's MCP connector with the URL and token below.",
    "mcp_url": "MCP endpoint URL",
    "deploy_token": "Deploy token",
    "public_base_url": "Public base URL",
    "copy_mcp_url": "Copy MCP URL"
  }
```

et dans `frontend/src/i18n/locales/fr.json` :

```json
  "settings": {
    "title": "Réglages",
    "mcp_intro": "Branchez le connecteur MCP de Claude avec l'URL et le token ci-dessous.",
    "mcp_url": "URL de l'endpoint MCP",
    "deploy_token": "Token de déploiement",
    "public_base_url": "URL publique de base",
    "copy_mcp_url": "Copier l'URL MCP"
  }
```

> Respecter la virgule JSON (ajouter le bloc en gardant un JSON valide ; le placer p.ex. après le bloc `detail`).

- [ ] **Step 9: Lancer les tests + lint + typecheck + build**

```bash
cd frontend
pnpm test -- settings
pnpm lint && pnpm typecheck && pnpm build
cd ..
```
Expected: test settings PASS ; lint/typecheck/build verts (`dist/` régénéré).

- [ ] **Step 10: Commit**

```bash
rtk git add frontend/src/hooks/use-settings.ts frontend/src/routes/settings.tsx frontend/src/routes/settings.test.tsx frontend/src/router.tsx frontend/src/test/utils.tsx frontend/src/components/topbar.tsx frontend/src/i18n/locales/en.json frontend/src/i18n/locales/fr.json
rtk git commit -m "✨ feat(settings): panneau Settings React (infos MCP, token masqué via PinField)"
```

---

## Task 7: Vérification end-to-end + cargo-deny

Garde-fous globaux : invariants sécu, garde d'architecture, supply-chain.

**Files:** aucun (vérification).

- [ ] **Step 1: Garde d'architecture (cœur sans axum/loco)**

Run: `cargo nextest run -p latch --test architecture`
Expected: PASS (le module `mcp/` est un adaptateur, pas dans `src/services/` → garde toujours verte).

- [ ] **Step 2: Suite backend complète**

Run: `cargo nextest run`
Expected: PASS (anciens + 6 MCP + 2 settings + openapi_drift à jour).

- [ ] **Step 3: fmt + clippy --all-features (parité CI)**

```bash
cargo fmt --all -- --check
cargo clippy --all-targets --all-features -- -D warnings
```
Expected: 0 warning.

- [ ] **Step 4: Supply-chain (nouvelle dep rmcp)**

Run: `cargo deny check`
Expected: PASS. Si un advisory/licence rmcp transitif bloque, le traiter (mise à jour, ou exception documentée dans `deny.toml` avec justification) — **ne pas** ignorer en masse.

- [ ] **Step 5: Front complet**

```bash
cd frontend && pnpm lint && pnpm typecheck && pnpm test && pnpm build && cd ..
```
Expected: tout vert.

- [ ] **Step 6: Commit (si fix de lint/deny)**

```bash
rtk git add -A
rtk git commit -m "✅ test(phase-5): vérification end-to-end MCP + settings (clippy/deny/tests verts)"
```

(Si aucun changement, sauter ce commit.)

---

## Task 8: Documentation & mémoire (définition de « terminé »)

**Files:** `docs/contrat-deploy.md`, `docs/ROADMAP.md`, `docs/INDEX.md`, `docs/HANDOFF.md`, `docs/ENVIRONMENT.md`, `docs/QUIRKS.md`, `docs/CONVENTIONS.md`, `.env.example`.

- [ ] **Step 1: Contrat** (`docs/contrat-deploy.md`)
  - §5 : préciser les réponses des tools (`deploy_prototype` → `{url, version, code_protected}` ; `list_projects` → `{slug, name, code_protected, active_version}`), le slug devant préexister (pas d'auto-création), `activate` défaut `true`, `LATCH_PUBLIC_BASE_URL` source de l'hôte public + `allowed_hosts` dérivé.
  - §9 : note explicite — `GET /api/settings` (AdminAuth) expose `deploy_token` à l'admin authentifié ; invariants §9.1 (pas de hash) et §9.2 (PIN jamais en liste/MCP) restent vrais (le token n'est ni hash ni PIN, et n'apparaît jamais via MCP).

- [ ] **Step 2: ROADMAP** (`docs/ROADMAP.md`) — Phase 5 ✅ LIVRÉE (date 2026-06-25) + pointer la spec/plan. Mentionner le panneau Settings minimal livré (locale/thème restent Phase 7).

- [ ] **Step 3: INDEX** (`docs/INDEX.md`) — lignes livrables : helpers config (`deploy_token`/`public_base_url`/`host_authority`), `mcp/` (deploy_prototype + list_projects, gate token), `GET /api/settings`, panneau Settings React, cocher Phase 5.

- [ ] **Step 4: ENVIRONMENT** (`docs/ENVIRONMENT.md`) — ajouter `LATCH_PUBLIC_BASE_URL` (runtime, fail-secure, dérive `allowed_hosts`), préciser `DEPLOY_TOKEN` fail-secure, path `/mcp` (déjà esquissé), connexion connecteur Claude (URL + token via panneau Settings).

- [ ] **Step 5: QUIRKS** (`docs/QUIRKS.md`) — pièges rmcp 1.4 rencontrés (chemins d'import du transport/config/session, construction de `StreamableHttpServerConfig`, signature `ErrorData`, macros `#[tool_router]`/`#[tool_handler]` exigeant le champ `tool_router`).

- [ ] **Step 6: CONVENTIONS** (`docs/CONVENTIONS.md`) — skeleton adaptateur MCP (gate token → service → `map_core_err`), pattern `useSettings`, réutilisation `PinField` pour secret générique.

- [ ] **Step 7: `.env.example`** — ajouter sous le bloc MCP :

```bash
# --- URL publique (distribution) : source de vérité de l'hôte public ---
# Sert à MCP (URL renvoyée par deploy_prototype) et à allowed_hosts (rmcp >= 1.4).
# REQUIS en prod (le boot refuse de démarrer si absent). En dev : http://localhost:<PORT>.
LATCH_PUBLIC_BASE_URL=https://latch.owlnext.fr
```

- [ ] **Step 8: HANDOFF** (`docs/HANDOFF.md`) — entrée datée en haut : Dernière chose faite (Phase 5 livrée), Trucs en suspens (branchement réel Claude web à confirmer — déduit de la doc, cf. ROADMAP §5), Prochaine chose (Phase 6 e2e/durcissement ou Phase 7 peaufinage), Notes pour future Claude (pièges rmcp).

- [ ] **Step 9: Commit**

```bash
rtk git add docs/ .env.example
rtk git commit -m "📝 docs: Phase 5 livrée — contrat §5/§9, ROADMAP, INDEX, ENVIRONMENT, QUIRKS, CONVENTIONS, HANDOFF"
```

---

## Self-Review (rempli par l'auteur du plan)

**Spec coverage :**
- Slug préexistant → Task 3 (`deploy_unknown_slug_is_error`). ✅
- `activate` défaut true → Task 3 (`deploy_creates_version_and_activates_by_default`). ✅
- Réponse `{url, version, code_protected}` → Task 3. ✅
- `list_projects` sans PIN/hash → Task 4. ✅
- Token sur les 2 tools → Task 3 + Task 4. ✅
- `LATCH_PUBLIC_BASE_URL` fail-secure + `allowed_hosts` dérivé → Task 1 + Task 2. ✅
- `DEPLOY_TOKEN` fail-secure → Task 1 + boot Task 2. ✅
- `GET /api/settings` (AdminAuth) → Task 5. ✅
- Panneau Settings React (PinField, topbar) → Task 6. ✅
- Tests (gate, version créée, slug inconnu, invariant, settings 401) → Tasks 3-6. ✅
- Contrat/mémoire → Task 8. ✅

**Placeholder scan :** aucun « TBD/TODO ». Les notes « vérifier au cargo build » concernent des symboles rmcp et fournissent l'alternative concrète — pas des placeholders.

**Type consistency :** `LatchMcp::new(db, storage, deploy_token, public_base_url)` cohérent Task 2↔3↔tests ; `check_token` défini Task 3, réutilisé Task 4 ; `map_core_err` Task 2, utilisé Tasks 3-4 ; `SettingsResponse{deploy_token, mcp_url, public_base_url}` cohérent backend (Task 5) ↔ test front (Task 6). ✅
